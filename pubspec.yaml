name: demo_firebase_connection
description: A new demo Flutter project for connection Firebase
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.3
  firebase_auth: ^2.0.0
  cloud_firestore: ^2.3.0
  cloud_functions: ^2.0.0
  firebase_core: ^1.3.0
  firebase_crashlytics: ^2.0.7
  firebase_storage: ^9.0.0
  firebase_messaging: ^10.0.3
  firebase_remote_config: ^0.10.0+2
  google_fonts: ^2.1.0
  provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.1

flutter:
  uses-material-design: true

  assets:
    - assets/
